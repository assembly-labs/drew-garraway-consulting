/**
 * Data barrel export
 * Import from here for cleaner imports:
 *
 * import { currentMember, trainingStats } from '../data';
 * import mockData from '../data';
 */

export {
  default as mockData,
  currentMember,
  membershipInfo,
  paymentHistory,
  upcomingCharges,
  trainingStats,
  techniques,
  milestones,
  scheduleDays,
  classFilters,
  scheduleClasses,
  todayClasses,
  nextClass,
  classDetailExample,
  instructors,
  classAttendees,
  initialCheckInHistory,
  getMemberDisplayInfo,
  getFilteredClasses,
  getInstructorByName,
} from './mockData';
