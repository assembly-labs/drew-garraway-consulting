# Claude API Configuration for Local Development
# NOTE: This is now only used locally. In production, the API key is stored
# securely in Cloudflare's environment variables and accessed by the Workers function.

# For local development
CLAUDE_API_KEY=your-anthropic-api-key-here

# Optional: Set to 'production' for production builds
VITE_ENV=development