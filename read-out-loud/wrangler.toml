# Cloudflare Pages configuration for Read Out Loud

name = "read-out-loud"
compatibility_date = "2025-01-01"
pages_build_output_dir = "."

# No build command needed - static vanilla JS PWA
# Headers configured in _headers file
# Redirects configured in _redirects file
