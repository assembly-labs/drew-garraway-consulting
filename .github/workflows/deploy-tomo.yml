name: Deploy TOMO to Cloudflare Pages

on:
  push:
    branches:
      - main
    paths:
      - 'TOMO/prototype/**'
      - '.github/workflows/deploy-tomo.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy TOMO Prototype

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: TOMO/prototype/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: TOMO/prototype

      - name: Build
        run: npm run build
        working-directory: TOMO/prototype

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy dist --project-name=bjjj
          workingDirectory: TOMO/prototype
