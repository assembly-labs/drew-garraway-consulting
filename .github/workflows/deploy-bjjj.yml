name: Deploy BJJJ to Cloudflare Pages

on:
  push:
    branches:
      - main
    paths:
      - 'BJJJ/prototype/**'
      - '.github/workflows/deploy-bjjj.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy BJJJ Prototype

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: BJJJ/prototype/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: BJJJ/prototype

      - name: Build
        run: npm run build
        working-directory: BJJJ/prototype

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN_BJJJ }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_BJJJ }}
          command: pages deploy dist --project-name=bjjj
          workingDirectory: BJJJ/prototype
