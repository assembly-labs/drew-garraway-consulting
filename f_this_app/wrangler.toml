# Cloudflare Pages configuration for F*** This App!

name = "f-this-app"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

# Pages configuration (Next.js static export outputs to 'out')
pages_build_output_dir = "out"

# Note: For full SSR support, consider migrating to OpenNext adapter
# https://opennext.js.org/cloudflare
