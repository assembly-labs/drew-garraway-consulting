# Cloudflare Pages configuration for NoFomo
# Vite SPA deployment

name = "nofomo"
compatibility_date = "2024-01-01"

# Pages configuration - Vite builds to dist/
pages_build_output_dir = "dist"

[vars]
NODE_ENV = "production"
